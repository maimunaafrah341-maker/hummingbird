# Copy to .env and fill in. .env must never be committed.
#
# Only translation.py needs a key. language.py needs none at all --
# detection runs entirely locally.

# At least ONE of the three below. llm.py tries them in this order and
# skips any that is unset, so one key is enough to run. These power
# generate_response(), which is what /translate uses -- and what
# /incident uses to localize its spoken alert.
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Required by /incident. Powers generate_structured_response(), which
# calls Featherless (Qwen2.5-72B-Instruct) with NO fallback chain --
# unlike the three keys above, this one has no substitute, and without
# it /incident cannot produce a response at all.
FEATHERLESS_API_KEY=

# Optional. Unset means the API is open, which is right for a public
# demo. Set it and every endpoint except /health requires a matching
# X-API-Key header.
API_KEY=

# Optional. Comma-separated browser origins allowed to call this API,
# or "*" for any. Only matters when a web page on another origin calls
# it -- server-to-server callers are unaffected, because CORS is
# enforced by browsers and nothing else. Defaults to the deployed
# console; set this when the console moves or a Vercel preview build
# gets its own hostname.
CORS_ORIGINS=

# Optional. Set to 1 to load the embedding model at startup instead of
# on the first romanized request. Costs ~22s and ~800MB before the port
# opens -- leave it unset on a small host, where that is exactly what
# gets a container killed during boot. See EVAL.md.
WARM_UP=
